import { Navbar } from "./Navbar"

export const Header = () => {

    return (
        <header className="flex fixed w-full bg-header items-center justify-center h-16">
            <div className="flex w-full container items-center justify-between px-4 sm:px-8 md:px-12">
                oi

                <Navbar />

                oi
            </div>
        </header>
    )
}